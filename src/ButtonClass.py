import pygamefrom src.config_screen import screen_screen = screen_()class Button:    """    Класс, представляющий кнопку в Pygame.    Атрибуты:    image (pygame.Surface): Изображение кнопки.    rect (pygame.Rect): Прямоугольник, определяющий размеры и положение кнопки.    clicked (bool): Флаг, указывающий, была ли нажата кнопка.    """    def __init__(self, x, y, image):        """        Инициализирует кнопку с заданными координатами и изображением.        Параметры:        x (int): Координата X верхнего левого угла кнопки.        y (int): Координата Y верхнего левого угла кнопки.        image (pygame.Surface): Изображение кнопки.        """        self.image = image        self.rect = self.image.get_rect()        self.rect.x = x        self.rect.y = y        self.clicked = False    def draw(self):        """        Отображает кнопку на экране и обрабатывает нажатия мыши.        Возвращает:        bool: True, если кнопка была нажата, иначе False.        """        action = False        # Получение позиции мыши        pos = pygame.mouse.get_pos()        # Проверка условий наведения и нажатия        if self.rect.collidepoint(pos):            if pygame.mouse.get_pressed()[0] == 1 and not self.clicked:                action = True                self.clicked = True        if pygame.mouse.get_pressed()[0] == 0:            self.clicked = False        # Отрисовка кнопки        screen.blit(self.image, self.rect)        return action